[pre_model_sync]
# Patches that run before doctypes are synced. Both sections must exist —
# `bench migrate` refuses to start if one is missing, even when it is empty.

[post_model_sync]
# Patches that run after doctypes are synced. Most patches belong here.
