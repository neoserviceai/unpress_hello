[project]
name = "unpress_hello"
version = "0.1.0"
description = "An example Unpress plugin"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
